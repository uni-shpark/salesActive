<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="com.uni.sellers.clientsalesactive.mapper.SalesActiveMapper">
 
    <select id="selectBizFileStore" resultType="hashmap">
        select * from biz_file_store where file_id=#{file_id}
    </select>
 
</mapper>